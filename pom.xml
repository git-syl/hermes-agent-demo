<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>hermes-agent-demo</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>hermes-agent-demo</name>
    <description>Hermes-style Spring AI agent runtime: multi-model routing, sandbox tools, dynamic skills, MCP, external platforms and Human-in-the-Loop</description>

    <properties>
        <java.version>25</java.version>
        <spring-ai.version>2.0.1</spring-ai.version>
        <spring-ai-agent-utils.version>0.10.0</spring-ai-agent-utils.version>
        <agent-sandbox.version>0.10.0-SNAPSHOT</agent-sandbox.version>
        <!-- 3.5.13 起提供 Spring Boot 4 专用 starter（基于 mybatis-spring 4.0.0）。 -->
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- MyBatis-Plus BOM：统一管理 starter / jsqlparser / generator 等模块版本。 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-bom</artifactId>
                <version>${mybatis-plus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot 4 与 Spring AI 2.0.0-RC1 默认都已切到 Jackson 3 (tools.jackson.*)，
             但本项目自有代码仍在用 Jackson 2 (com.fasterxml.jackson.*)
             （rag / mymodel / tools/external 等模块）。显式引入 jackson2 自动配置模块，
             让容器继续提供 com.fasterxml.jackson.databind.ObjectMapper bean，
             全项目共用一份配置。后续逐步迁到 Jackson 3 即可移除该依赖。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-jackson2</artifactId>
        </dependency>

        <!-- Spring AI chat client -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-client-chat</artifactId>
        </dependency>

        <!-- Model starters: both Anthropic and OpenAI; routed at runtime by modelName -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-anthropic</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-deepseek</artifactId>
        </dependency>

        <!-- Spring AI MCP support (provides SyncMcpToolCallbackProvider). We build MCP clients
             manually per request, not via the autoconfigured starter. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-mcp</artifactId>
        </dependency>

        <!-- ===== RAG 知识库模块 ===== -->
        <!-- RetrievalAugmentationAdvisor / ContextualQueryAugmenter / MultiQueryExpander 等 RAG 编排 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-rag</artifactId>
        </dependency>
        <!-- QuestionAnswerAdvisor：把向量检索结果拼进 prompt
             （RC1 起 artifact 由 spring-ai-advisors-vector-store 改名为 spring-ai-vector-store-advisor） -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-vector-store-advisor</artifactId>
        </dependency>
        <!-- Tika：解析 WORD/PPT/HTML 等通用格式 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-tika-document-reader</artifactId>
        </dependency>
        <!-- PDFBox：按页解析 PDF -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-pdf-document-reader</artifactId>
        </dependency>
        <!-- Markdown：按标题/段落结构化切分（.md/.markdown），优于 Tika 纯文本 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-markdown-document-reader</artifactId>
        </dependency>
        <!-- pgvector 向量库（与参考实现一致）。注意：starter 不带 JDBC 与驱动，需显式补齐。 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-vector-store-pgvector</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- MySQL 驱动（第三数据源 mysql，库见 docker/mysql-db）。版本由 Spring Boot BOM 管理。 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- 开发期观测 SQL：p6spy 包一层 JDBC，打印真实 SQL + 实际参数值（配置见 spy.properties）。 -->
        <dependency>
            <groupId>p6spy</groupId>
            <artifactId>p6spy</artifactId>
            <version>3.9.1</version>
        </dependency>

        <!-- ===== MyBatis-Plus（仅服务第二数据源 ai-sport，装配见 com.example.chat.aisport）=====
             版本由上面的 mybatis-plus-bom 统一管理；jsqlparser 是分页/乐观锁插件解析 SQL 的必需模块。 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
        </dependency>

        <!-- Spring AI Community: SkillsTool for skills directory -> tool callbacks -->
        <dependency>
            <groupId>org.springaicommunity</groupId>
            <artifactId>spring-ai-agent-utils</artifactId>
            <version>${spring-ai-agent-utils.version}</version>
        </dependency>

        <!-- Agent Sandbox API + Docker backend.
             Provides real process/filesystem/network isolation for tool execution,
             required because skills are user-uploaded scripts running on a shared server.
             0.9.2 起项目从 spring-ai-community 迁到 markpollack：
               - groupId:  org.springaicommunity     → io.github.markpollack
               - 包名:     org.springaicommunity.sandbox.* → io.github.markpollack.sandbox.*
             0.10.0-SNAPSHOT 来自 central-portal-snapshots（仓库见下方 <repositories>）。 -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>agent-sandbox-core</artifactId>
            <version>${agent-sandbox.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>agent-sandbox-docker</artifactId>
            <version>${agent-sandbox.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
        <repository>
            <id>central-portal-snapshots</id>
            <name>Central Portal Snapshots</name>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
